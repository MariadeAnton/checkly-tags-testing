# checkly-tags-testing

Quick test repo to show how we want to filter browser checks from a playwright spec file.

```sh
npx checkly test -v
```

See the [check file](./__checks__/test.check.ts) for what we want to change.
